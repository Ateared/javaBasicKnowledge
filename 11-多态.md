## 多态

```XML
继承： 多态在子类与父类之间
覆盖： 子类重写了父类的成员方法
声明的变量类型是父类类型  但实例指向子类实例
多态发生时：
	java 虚拟机 运用时根据引用变量指向的实例调用它的方法 而不是根据引用变量的类型来调用

向下转型：
	Cat c = (Cat)a;        // 向下转型
	将父类引用类型变量转换为子类类型  需要强制转换
向上转型：
	Animal a = new Cat();  // 向上转型
	将子类引用类型变量转换为父类类型

转型异常：
	classCastException
```

